## 📦 Day 2 — Collections & Dataclasses

🎯 Цель дня:

- Уверенно выбирать структуру данных под задачу (как на интервью)
- Знать `collections` (defaultdict/Counter/deque) и типичные ловушки
- Уметь делать DTO/Value Objects через `dataclass` (safe defaults, immutability)
- Понимать сложности операций (хотя бы базово)

---

### ✅ Checklist Day 2

- [ ] list / tuple / set / dict — свойства и применение
- [ ] Базовые сложности операций (O(1) / O(n))
- [ ] `collections`: `defaultdict`, `Counter`, `deque`
- [ ] Типичные ловушки collections
- [ ] `dataclass`: init/eq/repr
- [ ] `field(default_factory=...)` и safe defaults
- [ ] `frozen=True` (immutability)
- [ ] `slots=True` (оптимизация)
- [ ] Мини-практика: Counter / deque / dataclass DTO
- [ ] Mini-test (10 вопросов)

---

## 1️⃣ list / tuple / set / dict (основы + применение)

📖 Материалы:

- Built-in types overview: https://docs.python.org/3/library/stdtypes.html
- Data structures tutorial: https://docs.python.org/3/tutorial/datastructures.html

### 🧠 Кратко

- `list` — упорядоченная mutable коллекция
- `tuple` — упорядоченная immutable коллекция
- `set` — уникальные элементы, быстрый membership
- `dict` — key/value, быстрый доступ по ключу, insertion-ordered (>=3.7)

[Code Sample](collection.py)

---

## 2️⃣ Сложности операций (минимум для интервью)

📖 Материалы:
Time complexity (официально, кратко): https://wiki.python.org/moin/TimeComplexity

🧠 Must-know (упрощённо)  
list:  
append — амортизированно O(1)  
x in list — O(n)  
insert(0, x) — O(n)  
dict/set:  
get / in — обычно O(1)  
tuple:  
x in tuple — O(n)

✍️ Задания:  
Объяснить, почему insert(0, x) медленный на list  
Потому что list — это не связный список, а динамический массив, и при вставке в начало нужно сдвинуть все элементы вправо.

Объяснить, почему dict/set дают O(1) lookup (hash table)

## Почему `list.insert(0, x)` — медленная операция

В Python `list` реализован как **динамический массив**, а не как связный список.

Элементы хранятся **непрерывно в памяти**, поэтому при вставке в начало списка:
- необходимо освободить место под новый элемент
- **все существующие элементы сдвигаются вправо на одну позицию**

Так как требуется переместить `n` элементов, сложность операции:
insert(0, x) → O(n)

По этой же причине:
- `append()` — `O(1)` (амортизированно)
- `pop()` с конца — `O(1)`
- `pop(0)` — `O(n)`

---

## Почему `dict` и `set` дают `O(1)` lookup

`dict` и `set` в Python реализованы как **хеш-таблицы**.

### Как происходит поиск:

1. Вычисляется `hash(key)`
2. Хеш преобразуется в индекс внутреннего массива
3. По этому индексу выполняется прямой доступ к элементу

Так как:
- вычисление хеша — `O(1)`
- доступ к массиву — `O(1)`
- нет линейного перебора элементов

поиск выполняется за:
lookup → O(1) (в среднем)

### Коллизии

В случае коллизий Python использует:
- open addressing
- probing

Хеш-таблица поддерживается разреженной и автоматически перераспределяется (rehash),  
что позволяет сохранять **O(1)** сложность в среднем случае.

---

## Краткое сравнение структур данных

| Структура | Lookup | Вставка в начало |
|----------|--------|------------------|
| `list`   | O(n)   | O(n)             |
| `dict`   | O(1)   | O(1)             |
| `set`    | O(1)   | O(1)             |
| `deque`  | O(n)   | O(1)             |

🎤 Интервью-вопросы:
Почему list append быстрый?
Почему dict/set не гарантируют O(1) в худшем случае?
Что такое hash collision и как она влияет?
---

## 3️⃣ collections — must know

📖 Материалы:
collections module: https://docs.python.org/3/library/collections.html

### 3.1 defaultdict

✍️ Задания:
Перегруппировать список пар (key, value) в dict[list]  
Сделать то же самое без defaultdict и сравнить читаемость

🎤 Интервью-вопросы:  
Когда defaultdict полезен?  
1) **Группировка (group by)**
2) **Счётчики / частоты (если не нужен Counter)**
3) **Мультисет / индексация по ключу**
4) **Графы / adjacency list**
5) **Составные структуры “на лету”**
Почему defaultdict может скрывать баги?
1) **Опечатка в ключе создаёт новый ключ вместо ошибки**
2) **Проверка существования ключа через доступ добавляет ключ**
3) **Неожиданное “разрастание” словаря**
4) **Сложнее ловить ошибки контракта**

Практические рекомендации

Используй defaultdict, когда создание значения при первом доступе — ожидаемое поведение (группировки, счётчики, графы).

Если отсутствие ключа — это ошибка/событие, лучше:
обычный dict + явная инициализация через setdefault, get, if key in d
или бросать/логировать ошибку при отсутствии ключа.  

````python
k = 'a'
v = [1, 2]
d = {}
d.setdefault(k, []).append(v)
````

3.2 Counter

✍️ Задания:  
Написать top_k_words(text, k) через Counter

🎤 Интервью-вопросы:  
Чем Counter лучше ручного dict-счётчика?  
Как получить топ-N? - most_common(N) — самый простой способ получить top-N

**Когда Counter — лучший выбор**  
подсчёт частот  
лог-аналитика  
статистика  
обработка текста  
агрегирование событий  

**Когда лучше обычный dict**  
нужна строгая логика ошибок при отсутствии ключа  
сложные значения, не просто числа  
важен полный контроль над поведением  

### 3.3 deque

✍️ Задания:  
Реализовать FIFO очередь: append + popleft
Реализовать “last N events” через deque(maxlen=N)

🎤 Интервью-вопросы:  
Почему deque лучше list для очереди?
Для чего maxlen?

[Code Sample](3.py)
---

## 4️⃣ Типичные ловушки collections (обязательный блок)

✍️ Задания:  
Объяснить обе ловушки  
Написать исправленный код

🎤 Интервью-вопросы:  
Почему [[...]] * n опасно?  
Почему нельзя менять dict при итерации?

[Code Sample](4.py)
---

## 5️⃣ dataclasses — must have для DTO/Value Objects

📖 Материалы:
dataclasses: https://docs.python.org/3/library/dataclasses.html  
RealPython (обзор): https://realpython.com/python-data-classes/

5.1 Базовый @dataclass
5.2 Safe defaults (важнейшая ловушка)
5.3 Immutability (frozen=True)
5.4 slots=True (оптимизация)

✍️ Задания:  
Создать dataclass DTO User(id, email, is_active, roles)
Сделать roles safe defaults  
Сделать вариант immutable (frozen)  
Объяснить, что даёт slots  

## 🎤 Интервью-вопросы:  
## Когда `@dataclass` лучше обычного класса

`dataclass` удобен, когда класс — это в первую очередь **контейнер данных**, а не объект со сложной логикой.

### Подходит, если нужно:
- быстро описать структуру данных (DTO/Request/Response/Config)
- автоматически получить:
    - `__init__`
    - `__repr__`
    - `__eq__` (и сравнение)
- легко делать типизацию и читаемый код

```python
from dataclasses import dataclass

@dataclass
class User:
    id: int
    name: str
```

## Почему нельзя roles=[]?  
Потому что значение по умолчанию вычисляется один раз при определении класса/функции,
и этот один и тот же список будет разделяться всеми экземплярами.  

## Что даёт frozen=True?  
frozen=True делает экземпляры неизменяемыми (примерно как immutable):  

## Что даёт slots=True?
slots=True включает __slots__ для dataclass (Python 3.10+), что даёт:
меньше памяти на экземпляр (нет __dict__ по умолчанию)
часто быстрее доступ к атрибутам
нельзя “случайно” добавить новый атрибут:

[Code Sample](5.py)

---
✅ Практика дня (3 задачи)
🧪 Task 1 — Word frequency (Counter)  
Реализовать word_stats(text: str) -> dict[str, int] через Counter  
Нормализовать регистр, игнорировать пунктуацию (bonus)  

🧪 Task 2 — Last N events (deque)  
Реализовать LastNQueue(maxlen=N) с push() и items()

🧪 Task 3 — DTO via dataclass
Реализовать immutable UserDTO (frozen + safe defaults) 
Добавить метод with_added_role(role) (создаёт новый объект)

---

🧪 Mini-Test — Day 2 (10 вопросов + ответы)  

❓ 1) Чем list отличается от tuple?
<details><summary>Ответ</summary>list mutable, tuple immutable; tuple можно хешировать при immutable элементах</details>
❓ 2) Почему x in set быстрее, чем x in list?
<details><summary>Ответ</summary>set — hash table, membership обычно O(1), list — O(n)</details>
❓ 3) Что вернёт {"a":1, "a":2}?
<details><summary>Ответ</summary>{"a": 2} — ключ перезаписывается</details>
❓ 4) Почему [[0]*3]*3 — ловушка?
<details><summary>Ответ</summary>все строки — ссылки на один и тот же список</details>
❓ 5) Как правильно задать пустой список в dataclass?
<details><summary>Ответ</summary>`field(default_factory=list)`</details>
❓ 6) Что делает Counter.most_common(k)?
<details><summary>Ответ</summary>возвращает топ-k элементов по частоте</details>
❓ 7) Чем deque лучше list для очереди?
<details><summary>Ответ</summary>`popleft()` у deque O(1), у list pop(0) O(n)</details>
❓ 8) Почему нельзя менять dict во время итерации по нему?
<details><summary>Ответ</summary>изменяется размер/структура — RuntimeError</details>
❓ 9) Что даёт frozen=True в dataclass?
<details><summary>Ответ</summary>immutability, безопасный `__hash__` при immutable полях</details>
❓ 10) Что гарантирует dict по порядку?
<details><summary>Ответ</summary>порядок вставки (Python 3.7+ как гарантия языка)</details>